package hpack;

import org.joda.time.DateTimeUtils;

/**
 * Hello world!
 *
 */
public class DH
{
    public static void showCurrentTime(String input)
    {
        System.out.println( input + ": " + DateTimeUtils.currentTimeMillis());
    }
}
